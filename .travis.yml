language: haskell

install:
  - mkdir -p ~/.local/bin
  - export PATH=~/.local/bin:$PATH
  - travis_retry curl -L https://www.stackage.org/stack/linux-x86_64 | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'
  - chmod +x ~/.local/bin/stack

script:
  - stack setup --no-terminal
  - stack build --fast --no-terminal

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: $PROJECT_ROOT/doc/html/studienarbeit-flagchecker-1.0.0.0
  github_token: $GITHUB_TOKEN
  on:
    branch: master
#/home/travis/build/ob-fun-ws17/studienarbeit-flagchecker/.stack-work
cache:
  timeout: 2000
  directories:
  - $HOME/.cabal
  - $HOME/.ghc
  - $HOME/.stack
  - .stack-work/
  apt: true
